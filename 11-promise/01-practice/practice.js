//Ebben a feladatban a Promise-t kell gyakorolod
//Írj egy rövid programot, amely a hasEnoughMoney (boolean) változó szerint eldönti,
//hogy tudunk-e új telefont vásárolni, vagy sem.
//Ha igen, akkor a létrehozott új telefon brand és color fieldeit írjuk ki a konzolra,
//Ha nem, akkor csak annyi az üzenet, hogy erre nincs elég pénz.
//Nem kell html file, simán futtasd a kódot node-on (node + relative path)

const hasEnoughMoney = false;

const canBuyNewPhone = new Promise(
    //A Prmise két ága: resolve és reject 
    //Hint: if-else
   
);

const print = function (newPhone) {
    //Írja ki a függvény a telefon adatait
    
};

const checkOptions = function () {
    //Itt hívd meg a canBuyNewPhone Promise-t, a then ágon írjk ki az adatokat, és gondoskodj catch ágról is
   
};

checkOptions();